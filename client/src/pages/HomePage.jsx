export default function HomePage() {
  return (
    <section className="page card hero-card">
      <h1>PackThreads</h1>
      <p>
        Open graphic t-shirt packs, collect rare drops, and climb the
        leaderboard.
      </p>
    </section>
  );
}